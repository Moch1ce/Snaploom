# 从主分支整体替换 .NET 实现

> 状态：已由 [ADR 0005](./0005-restore-dotnet-avalonia.md) 取代；本文仅保留历史决策背景。

Tauri 2 重构开始前，先在最后一个可运行的 .NET/Avalonia 提交创建并推送 `dotnet-final` 标签；随后从主分支删除旧项目，直接建立 Rust 与 TypeScript 实现，不维持两套可构建应用。该选择保持当前代码树和发布物纯粹，但失去并行回归基准，因此所有迁移调查与验收标准必须在删除前完成，历史行为仅通过标签和既有文档追溯。
